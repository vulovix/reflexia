{
  "name": "@reflexia/cli",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@reflexia/cli:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@reflexia/cli:build:development"
          },
          "production": {
            "buildTarget": "@reflexia/cli:build:production"
          }
        }
      }
    }
  }
}
